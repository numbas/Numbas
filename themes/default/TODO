On remove-bootstrap:

* Look at whether the .correct-answer span can be defined just once.

* Add type-ahead to the sequential question list. Number moves to q with that number, letter characters moves to question with that name.
