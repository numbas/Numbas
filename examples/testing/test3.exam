{
	name: Hullo2
	questions: [
		{
			name: Hullo21
			variables: {
				a: -random(1..9)
				b: random(1..9)
				c: random(1..9)
				}	
			statement: "Given the following relation between $x$ and $y$
			\[\simplify{x^2+y^2+{a}x+{b}y}=\var{c}\]
			answer the following question."
			parts: [
				{
					type: GapFill
					prompt: """Using implicit differentiation find $\frac{dy}{dx}$ in terms of $x$ and $y$.
					Input your answer here:
					
					$\frac{dy}{dx}=$ [[0]]"""
					gaps: [ 
						{type: jme, answer: "(({( - a)} + ( - (2 * x))) / ({b} + (2 * y)))" , marks: 3.5}
						]
				}	
			]
			advice: 
					"""On differentiating both sides of the equation implicitly we get
					\[2x + \simplify[1111110111111111]{2y*Diff(y,x,1) + {a} + {b} *Diff(y,x,1)} = 0\]
					Collecting terms in $\frac{dy}{dx}$ and rearranging the equation we get 
					\[(\var{b} + 2y) \frac{dy}{dx} = \simplify{{ -a} -2x}\] and hence on further rearranging:
					\[\frac{dy}{dx} = \simplify{({ - a} - 2 * x) / ({b} + (2 * y))}\]"""
		}
		{
			name: Hullo2
			variables: {
				a: "if(n=2,random(4,9,16,25,36),if(n=3,random(8,27,64),if(n=4,random(16,81),random(32,243))))"
				h: "precround(random(1,-1)*random(0.001..0.1#0.001),5)"
				n: "random(2..5)"
				est: "precround(a^(m/n)+h*m*a^(m/n-1)/n,5)"
				tr: "precround((a+h)^(m/n),5)"
				m: "if(n=2 or n=4,random(1,3,5), if(n=3,random(1,2,4,5),random(1,2,3,4,6)))"
				s1: "random(1,-1)"	
				c: "precround(a^(1/n),0)"
				p: "m-n"
				}	
			statement: "Use the approximation $f(a+h) \approx f(a)+hf\;'(a)$ to estimate \[\var{a+h}^{\frac{\var{m}}{\var{n}}} \]for a suitable function $f(x)$. 
			"
			parts: [
				{
					type: GapFill
					prompt: """In this case $f(x) =\;$ [[0]]		(input your answer to 5 decimal places).
					
					$a= \;$[[1]] and $h=\; $[[2]]
					Input your estimation to $5$ decimal places: [[3]]
					
					True value is: [[4]] (input to 5 decimal places)."""
					gaps: [ 
						{type: jme, answer: "x^({m}/{n})", marks: 0.5}
						{type: numberentry, answer: "{a}", marks: 0.25}
						{type: numberentry, answer: "{h}", marks: 0.25}
						{type: numberentry, answer: "{est}", marks: 2}
						{type: numberentry, answer: "{tr}", marks: 1}
					]
				}	
			]
			advice: 
					""" We have $f(x)=x^\frac{\var{m}}{\var{n}}$ and $a= \var{a}$, $h=\var{h}$.
					Note that $\var{a}^\frac{1}{\var{n}}=\var{c}$ and so using the approximation :
					$f(a+h)\approx f(a)+h\;f\;'(a)$ and $f\;'(x) = \frac{\var{m}}{\var{n}}\simplify[[1111110111111110]{x^({m-n}/{n})}$ 
					we have:
					\[\simplify[1111110111111110]{{a+h}^({m}/{n})}\approx \simplify[simplifyFractions]{{a}^({m}/{n})+{h}*({m}/{n})*{a}^({p}/{n})}=\simplify[1111110111110110]{{c}^{m}+ {h}*({m}/{n})*{c}^{m-n}}=\var{est}\]
					to 5 decimal places.
					The true value to 5 decimal places is {tr}.
					"""
		}
		{
			name: Hullo3
			variables: {
				a: "random(1..4)"
				b: "s1*random(1..5)"
				n: "random(2..6)"
				m: "random(2..8)"
				s1: "random(1,-1)"	
				}	
			statement: "Differentiate the following function $f(x)$ using the product rule." 
			parts: [
				{
					type: GapFill
					prompt: """
					$\simplify[111111011111111]{f(x) = ({a} + {b} * x) ^ {m} * e ^ ({n} * x)}$
					
					You are given that \[\simplify[111111011111111]{Diff(f,x,1) = ({a} + {b} * x) ^ {m -1} * e ^ ({n} * x) * g(x)}\]
					
				    for a polynomial $g(x)$. You have to find $g(x)$. 
					
					$g(x)=\;$[[0]]"""
					gaps: [ 
						{type: jme, answer: "({((m * b) + (n * a))} + ({(n * b)} * x))", marks: 3}
					]
					steps: [
					{
						type: information
						prompt: """The product rule says that if $u$ and $v$ are functions of $x$ then
						\[\simplify{Diff(u * v,x,1) = u * Diff(v,x,1) + v * Diff(u,x,1)}\]"""
					}
					]
					stepspenalty: 1
				}	
			]
			advice: 
					""" 
					
					The product rule says that if $u$ and $v$ are functions of $x$ then
						\[\simplify{Diff(u * v,x,1) = u * Diff(v,x,1) + v * Diff(u,x,1)}\]
					
					For this example:
					
					\[\simplify{u = ({a} + {b} * x) ^ {m}}\Rightarrow \simplify{Diff(u,x,1) = {m * b} * ({a} + {b} * x) ^ {m -1}}\]
					
					\[\simplify{v = e ^ ({n} * x)} \Rightarrow \simplify{Diff(v,x,1) = {n} * e ^ ({n} * x)}\]
					
					Hence on substituting into the product rule above we get:
					
					\[\simplify{Diff(f,x,1) = {m * b} * ({a} + {b} * x) ^ {m -1} * e ^ ({n} * x) + {n} * ({a} + {b} * x) ^ {m} * e ^ ({n} * x) = ({a} + {b} * x) ^ {m -1} * ({m * b + n * a} + {n * b} * x) * e ^ ({n} * x)}\]
					
					The last step was to take out the common term $\simplify{({a} + {b} * x) ^ {m -1} * e ^ ({n} * x)}$.
					
					Hence \[\simplify{g(x) = {m * b + n * a} + {n * b} * x}\].
					
					"""
		}
		{
			name: Hullo4
			variables: {
				a: "random(1..8)"
				b: "if(2|a,random(-7..7#2),random(-8..8#2))"
				c: "random(1,3,5,7)"
				d: "if(a*d1=b*c,abs(d1)+1,d1)"
				d1: "s1*random(1..8)"
				s1: "random(1,-1)"	
				}	
			statement: "Differentiate the following function $f(x)$ using the quotient rule or otherwise." 
			parts: [
				{
					type: GapFill
					prompt: """
					\[\simplify[111111011111111]{f(x) = ({a} * x + {b}) / Sqrt({c} * x + {d})}\]
					
					You are given that \[\simplify[111111011111111]{Diff(f,x,1) = g(x) / (2 * ({c} * x + {d}) ^ (3 / 2))}\]
					
				    for a polynomial $g(x)$. You have to find $g(x)$. 
					
					You can click on Steps to get help. You will lose 1 mark if you do so.
					
					$g(x)=\;$[[0]]"""
					gaps: [ 
						{type: jme, answer: "(({(a * c)} * x) + {((2 * a * d) + ( - (c * b)))})", answersimplification: "1111111111111111", marks: 3}
					]
					steps: [
					{
						type: information
						prompt: """The quotient rule says that if $u$ and $v$ are functions of $x$ then
						\[\simplify{Diff(u/v,x,1)=(v * Diff(u,x,1) -(u * Diff(v,x,1))) / v ^ 2}\]"""
					}
					]
					stepspenalty: 1
				}	
			]
			advice: 
					""" 
					The quotient rule says that if $u$ and $v$ are functions of $x$ then
					
						\[\simplify{Diff(u/v,x,1)=(v * Diff(u,x,1) -(u * Diff(v,x,1))) / v ^ 2}\]
					
					For this example:
					
					\[\simplify{u = {a} * x + {b}}\Rightarrow \simplify{Diff(u,x,1) = {a}}\]
					
					\[\simplify{v = Sqrt({c} * x + {d})} \Rightarrow \simplify{Diff(v,x,1) = {c} / (2 * Sqrt({c} * x + {d}))}\]
					
					Hence on substituting into the quotient rule above we get:
					
					\[\simplify{Diff(f,x,1) = ({a} * Sqrt({c} * x + {d}) -(({a} * x + {b}) * Diff(v,x,1))) / ({c} * x + {d}) = ({a} * Sqrt({c} * x + {d}) -(({c} * ({a} * x + {b})) / (2 * Sqrt({c} * x + {d})))) / ({c} * x + {d}) = ({2 * a} * ({c} * x + {d}) -({c} * ({a} * x + {b}))) / (2 * ({c} * x + {d}) ^ (3 / 2)) = ({a * c} * x + {2 * a * d -(c * b)}) / (2 * ({c} * x + {d}) ^ (3 / 2))}\]
					
					
					Hence \[\simplify{g(x) = {a * c} * x + {2 * a * d -(c * b)}}\].
					
					"""
		}
	]		
}		
		